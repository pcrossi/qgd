# Auditorias e relatórios gerais

Esta pasta preserva auditorias transversais, relatórios de status, respostas
de revisão e consolidações históricas.

Quando houver divergência, prevalecem `memory.md`, `brain/` e os documentos
finais em `questoes/`.
