---
title: "Buckingham prefactor audit"
---

# Buckingham prefactor audit

The reduced formula used for comparison is:

$$
\Pi_G^{\rm GDQ}
=
\frac{\alpha^4(1+\alpha)}{\chi_{\rm Fano}}
e^{-1/(2\alpha)}.
$$

With:

$$
\chi_{\rm Fano}
=
\frac{3\sqrt{2}}{5}.
$$

It is applied to the dimensionless group:

$$
\Pi_G
=
\frac{GM_p^2}{\hbar c}.
$$

Thus:

$$
G_{\rm GDQ}
=
\frac{\hbar c}{M_p^2}
\frac{\alpha^4(1+\alpha)}{\chi_{\rm Fano}}
e^{-1/(2\alpha)}.
$$

## 1. Separation of factors

The factor $\alpha^4$ is interpreted as the reduced determinant of the two-dimensional Hermitian channel when the effective transmission is proportional to $\alpha^2I_2$:

$$
\det_{\mathbb C}(\alpha^2I_2)
=
\alpha^4.
$$

The factor $1+\alpha$ is a first-order effective correction. It should not be called a Chern class, because Chern classes are integrals.

The factor $\chi_{\rm Fano}=3\sqrt{2}/5$ represents a channel-motivated admittance. To become a metrological result, it must come from a spectral gluing operator.

## 2. Reading of the exponent

The exponent should not be read as a Yang-Mills instanton or as a new term in the action. In the reduced calculation it comes from the chain:

$$
\beta_E=2\pi R_H,
\qquad
\tau_\ast=\frac{\beta_E^2}{16},
\qquad
\lambda_{\rm ax}=\frac{2}{R^2}.
$$

Therefore:

$$
\Delta u_v
=
\tau_\ast\pi^2\lambda_{\rm ax}
=
\frac{\pi^4}{2}\frac{R_H^2}{R^2}.
$$

If the global gluing imposes:

$$
R=\pi^2\sqrt{\alpha}\,R_H,
$$

then:

$$
\Delta u_v=\frac{1}{2\alpha},
\qquad
\frac{\mathcal U_\ast}{\mathcal U_0}=e^{-1/(2\alpha)}.
$$

Therefore the exponent is conditional on global gluing. It was not derived from the isolated flat local bulk.

## 3. What should not be done

One must not add a posterior correction chosen to zero the error in $G$. That would turn a comparison into a fit.

The residue of about $0.262\%$ remains recorded as the difference between the reduced formula and the accepted metrology.

Nor should one:

1. interpret $1+\alpha$ as a literal Chern class;
2. hide flattening inside $\chi_{\rm Fano}$ and then apply another external Jacobian;
3. call the BPST/Yang-Mills route an official GDQ derivation;
4. use a posterior electromagnetic correction to remove the residue.

## 4. Next necessary calculation

The complete prefactor must come from:

$$
K_{\rm grav}^{\rm phys}
\to
\det{}'K_{\rm grav}^{\rm phys}
\to
\chi_{\rm Fano}^{\rm spec}
\to
\Pi_G.
$$

This is a future metrological program, not a structural gap of the chapter.
